<%= form_for @medication, :html => { :multipart => true }   do |f| %>
  <% if @medication.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@medication.errors.count, "error") %> prohibited this medication from being saved:</h2>

      <ul>
      <% @medication.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :dosage %><br>
    <%= f.text_area :dosage %>
  </div>
  <div class="field">
    <%= f.label :sideeffects %><br>
    <%= f.text_area :sideeffects %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
